import numpy as np
import mathplotlib.pyplot as plt
from time import clock

st = clock()

A = np.ones(n,n)
B = np.ones(n,n)
C = np.zeros(n,n)


for i in range(n):
    for j in range():
        for k in range():
            C[i, j] = A[j, k] + B[i, k] 


x = [1,2,3]
#plt.plot(X)
y = [3,4,5]

print "aqui para mi:" , x+y

end = clock()

print end - st



   